{
  "name": "@mcp-agent-eval/cli",
  "version": "0.1.0",
  "description": "CLI runner for mechanistic and epistemic MCP agent evaluations",
  "type": "module",
  "bin": {
    "run-suite": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\"",
    "run-suite": "node dist/main.js"
  },
  "dependencies": {
    "@mcp-agent-eval/runner": "0.1.0",
    "commander": "^13.1.0"
  }
}
