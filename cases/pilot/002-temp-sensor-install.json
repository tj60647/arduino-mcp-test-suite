{
  "schemaVersion": "0.1.0",
  "id": "002-temp-sensor-install",
  "title": "Temperature sensor with library install",
  "category": "deterministic",
  "difficulty": "medium",
  "tags": ["compile", "dependency", "sensor"],
  "prompt": "Generate a sketch that reads a DS18B20 and prints Celsius over Serial.",
  "context": {
    "board": "arduino:avr:nano",
    "constraints": ["Use OneWire workflow", "Include serial output every second"],
    "assumptionsAllowed": true
  },
  "requiredCapabilities": ["project_init", "file_write", "dependency_install", "compile"],
  "objectiveChecks": [
    { "id": "cap-dep", "type": "requires_capability", "value": "dependency_install", "required": true, "weight": 0.4 },
    { "id": "cap-compile", "type": "requires_capability", "value": "compile", "required": true, "weight": 0.4 },
    { "id": "prompt-serial", "type": "requires_prompt_contains", "value": "Serial", "required": true, "weight": 0.2 }
  ]
}
